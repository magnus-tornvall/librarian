---
description: Replay what librarian pushed into this session and why
allowed-tools: Bash(librarian why-summary:*)
---

Assumes a shell-locatable `librarian` (on PATH, or replace with an absolute path).
Resolves to the newest active session in the traces; use `librarian why-summary --session <id>` at the CLI to target another.

!`librarian why-summary`
